[package]
name = "signedby-napi"
version = "0.1.0"
edition = "2021"
description = "Node.js bindings for SIGNEDBYME SDK"
license = "SSAL-1.0"

[lib]
crate-type = ["cdylib"]

[dependencies]
# napi-rs for Node.js bindings
napi = { version = "2", features = ["async", "napi9"] }
napi-derive = "2"

# Core SDK (local path) - note: package renamed to signedby-sdk but lib name is signedby_sdk
signedby_sdk = { path = "../../../native/signedby_core", package = "signedby-sdk" }

# Async runtime
tokio = { version = "1", features = ["rt-multi-thread"] }

# Serialization
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

# HTTP client
reqwest = { version = "0.12", features = ["json", "rustls-tls"], default-features = false }

[build-dependencies]
napi-build = "2"

[profile.release]
lto = true
opt-level = "z"
